@model VgcCollege.Web.Models.AssignmentResult

@{
    ViewData["Title"] = "Assignment Result Details";
}

<h1>Assignment Result Details</h1>

<hr />

<dl class="row">
    <dt class="col-sm-2">Student</dt>
    <dd class="col-sm-10">@Model.StudentProfile?.Name</dd>

    <dt class="col-sm-2">Assignment</dt>
    <dd class="col-sm-10">@Model.Assignment?.Title</dd>

    <dt class="col-sm-2">Course</dt>
    <dd class="col-sm-10">@Model.Assignment?.Course?.Name</dd>

    <dt class="col-sm-2">Branch</dt>
    <dd class="col-sm-10">@Model.Assignment?.Course?.Branch?.Name</dd>

    <dt class="col-sm-2">Score</dt>
    <dd class="col-sm-10">@Model.Score</dd>

    <dt class="col-sm-2">Feedback</dt>
    <dd class="col-sm-10">@Model.Feedback</dd>
</dl>

@if (User.IsInRole("Administrator") || User.IsInRole("Faculty"))
{
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Edit</a>
}

@if (User.IsInRole("Student"))
{
    <a asp-action="MyResults" class="btn btn-secondary">Back</a>
}
else
{
    <a asp-action="Index" class="btn btn-secondary">Back</a>
}
